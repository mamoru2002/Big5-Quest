<% confirmation_url = api_user_credential_confirmation_url(confirmation_token: @token) %>

<p><%= @resource.email %> 様</p>

<p>BIG5-Quest にご登録いただきありがとうございます。以下のボタンを押してメールアドレスの確認を完了すると、サービスを開始できます。</p>

<p style="margin: 24px 0; text-align: center;">
  <a href="<%= confirmation_url %>" style="display: inline-block; padding: 12px 24px; border-radius: 9999px; background-color: #00A8A5; color: #ffffff; text-decoration: none; font-weight: bold;">
    メールアドレスを確認する
  </a>
</p>

<p>ボタンが押せない場合は、次のURLをブラウザにコピーしてアクセスしてください。</p>
<p><a href="<%= confirmation_url %>"><%= confirmation_url %></a></p>

<p>※リンクの有効期限は一定時間で切れます。期限が切れた場合は、ログイン画面の「確認メールを再送する」から再度お手続きください。</p>
<p>※このメールに心当たりがない場合は破棄してください。</p>

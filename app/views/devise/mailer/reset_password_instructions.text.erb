<% frontend_base = ENV.fetch('FRONTEND_BASE_URL', 'https://app.big5-quest.com') %>
<% reset_url = "#{frontend_base}/reset?token=#{ERB::Util.url_encode(@token)}" %>

<%= @resource.email %> 様

BIG5-Quest のパスワード再設定を受け付けました。以下のリンクから再設定ページを開き、新しいパスワードを登録してください。

<%= reset_url %>

もしこのメールに心当たりがない場合は、本メールは破棄していただいて構いません。

※リンクの有効期限は一定時間で切れます。期限切れの場合は、もう一度パスワード再設定メールを送信してください。
